package pl.wojtyna.trainings.recipes.citybike.task.tracking.domain;

import java.util.UUID;

public class BikeLocationTrackingService {

    public void stopTrackingBike(UUID bikeId) {
        // noop
    }

    public void startTrackingBike(UUID bikeId) {
        // noop
    }

    public Location currentLocationOf(UUID bikeId) {
        // noop
        return new Location();
    }
}
